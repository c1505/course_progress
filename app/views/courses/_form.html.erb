<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_estimate %><br>
    <%= f.number_field :time_estimate %>
  </div>
  <div class="field">
    <%= f.label :time_completed %><br>
    <%= f.number_field :time_completed %>
  </div>
  <div class="field">
    <%= f.label :complete %><br>
    <%= f.check_box :complete %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :curriculum_id %><br>
    <%= f.number_field :curriculum_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
